# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The core agent instructions are consolidated for use across all our AI tools (Claude, agy, codex).
Claude Code does not read `AGENTS.md` on its own, so the line below imports it into context at
session start. It must stay a bare `@` import; a markdown link is not loaded.

@AGENTS.md

<!-- Claude-specific directives can be added below this line -->
